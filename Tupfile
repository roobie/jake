CFLAGS = -Wall -std=c++17 -D_REENTRANT
INCLUDE = -I/usr/include/SDL2 -I/usr/include/luajit-2.0 -I/usr/include/libdrm -I./imgui
LIBS = -lSDL2 -lGL

: foreach *.cpp |> clang++-7 $(CFLAGS) $(INCLUDE) -c %f -o %o |> %B.o
: *.o imgui/libimgui.a |> clang++ %f -o %o $(LIBS) |> jake